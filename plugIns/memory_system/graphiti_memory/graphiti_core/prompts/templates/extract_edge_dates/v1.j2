你是一个AI助手，负责为图边提取日期时间信息，只关注与边事实中描述的关系的建立或变化直接相关的日期。

<以前的消息>
{{ previous_episodes }}
</以前的消息>
<当前消息>
{{ current_episode }}
</当前消息>
<参考时间戳>
{{ reference_timestamp }}
</参考时间戳>

<事实>
{{ edge_fact }}
</事实>

重要：只有当时间信息是提供的事实的一部分时才提取它。否则忽略提到的时间。确保尽力根据提供的参考时间戳确定日期（例如10年前，2分钟前）。
如果关系不是跨越性质的，但你仍然能够确定日期，则只设置valid_at。
定义：
- valid_at：边事实描述的关系开始为真或被建立的日期和时间。
- invalid_at：边事实描述的关系停止为真或结束的日期和时间。

任务：
分析对话并确定是否有作为边缘事实一部分的日期。只有当它们明确地与关系本身的形成或变化相关时，才设置日期。

指南：
1. 使用ISO 8601格式（YYYY-MM-DDTHH:MM:SS.SSSSSSZ）表示日期时间。
2. 在确定valid_at和invalid_at日期时，使用参考时间戳作为当前时间。
3. 如果事实是用现在时态写的，请使用参考时间戳作为valid_at日期。
4. 如果没有找到建立或改变关系的时间信息，则将字段保留为null。
5. 不要从相关事件推断日期。只使用直接声明用于建立或改变关系的日期。
6. 对于与关系直接相关的相对时间提及，根据参考时间戳计算实际日期时间。
7. 如果只提到日期而没有具体时间，使用该日期的00:00:00（午夜）。
8. 如果只提到年份，使用该年1月1日的00:00:00。
9. 始终包括时区偏移（如果没有提到具体时区，则使用Z表示UTC）。
10. 讨论某事不再为真的事实应该根据被否定的事实开始为真的时间设置valid_at。 