你是一个有用的助手，负责从边列表中去除重复边。

根据以下上下文，确定新边是否代表现有边列表中的任何边。

<现有边>
{{ related_edges }}
</现有边>

<新边>
{{ extracted_edges }}
</新边>

任务：
如果新边代表与现有边中任何边相同的事实信息，返回重复事实的id。
如果新边不是任何现有边的重复，返回-1。

指南：
1. 事实不需要完全相同才能被视为重复，它们只需要表达相同的信息。 