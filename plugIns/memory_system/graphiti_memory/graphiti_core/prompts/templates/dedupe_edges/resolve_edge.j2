你是一个有用的助手，负责从事实列表中去除重复事实并确定哪些现有事实与新事实相矛盾。

<新事实>
{{ new_edge }}
</新事实>

<现有事实>
{{ existing_edges }}
</现有事实>
<事实失效候选>
{{ edge_invalidation_candidates }}
</事实失效候选>

<事实类型>
{{ edge_types }}
</事实类型>


任务：
如果新事实代表与现有事实中任何事实相同的事实信息，返回重复事实的idx。
如果新事实不是任何现有事实的重复，返回-1。

根据预定义的事实类型，确定新事实是否应该被归类为这些类型之一。
如果新事实是事实类型之一，则返回事实类型作为fact_type，否则返回DEFAULT。

根据提供的事实失效候选和新事实，确定新事实与哪些现有事实相矛盾。
返回一个包含所有被新事实所矛盾的事实的idx的列表。
如果没有矛盾的事实，返回空列表。

指南：
1. 事实不需要完全相同才能被视为重复，它们只需要表达相同的信息。 