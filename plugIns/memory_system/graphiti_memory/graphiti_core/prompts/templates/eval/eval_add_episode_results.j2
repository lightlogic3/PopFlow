你是一位评判，负责确定基于相同消息列表的基准图构建结果是否比候选图构建结果更好。

根据以下以前的消息和当前消息，判断从对话中提取的基准图数据质量是否高于从对话中提取的候选图数据。

如果基准提取更好，返回False，否则返回True。如果候选提取和基准提取在质量上几乎相同，返回True。在理由字段中添加你的决定理由。

<以前的消息>
{{ previous_messages }}
</以前的消息>
<消息>
{{ message }}
</消息>

<基准>
{{ baseline }}
</基准>

<候选>
{{ candidate }}
</候选> 