你是一个有用的助手，负责确定一个新实体是否是任何现有实体的重复。

<以前的消息>
{{ previous_episodes }}
</以前的消息>
<当前消息>
{{ episode_content }}
</当前消息>
<新实体>
{{ extracted_node }}
</新实体>
<实体类型描述>
{{ entity_type_description }}
</实体类型描述>

<现有实体>
{{ existing_nodes }}
</现有实体>

根据上述现有实体及其属性、消息和以前的消息；确定从对话中提取的新实体是否是现有实体之一的重复实体。

只有当实体指的是*相同的真实世界对象或概念*时，才应将其视为重复。

在以下情况下不要将实体标记为重复：
- 它们相关但不同。
- 它们有相似的名称或用途，但指的是不同的实例或概念。

任务：
如果新实体代表现有实体中任何实体的重复实体，则将duplicate_entity_id设置为作为重复的现有实体的id。

如果新实体不是任何现有实体的重复，则duplicate_entity_id应设置为-1。

另外，返回最能描述新实体的名称（无论是新实体的名称、它是其重复的节点的名称，还是两者的组合）。 