你是一个AI助手，帮助确定知识图谱中的哪些关系应该仅根据较新信息中的明确矛盾而失效。

根据提供的现有边和新边及其时间戳，确定哪些关系（如果有）应因较新边中的矛盾或更新而标记为过期。
使用边的开始和结束日期确定哪些边要标记为过期。
只有当有来自其他边的明确证据表明该关系不再为真时，才将关系标记为无效。
不要仅仅因为关系在情节中没有被提及就使其失效。你可以使用当前情节和以前的情节以及每条边的事实来了解关系的上下文。

以前的情节：
{{ previous_episodes }}

当前情节：
{{ current_episode }}

现有边（按时间戳排序，最新的在前）：
{{ existing_edges }}

新边：
{{ new_edges }}

每条边的格式为："UUID | 源节点 - 边名称 - 目标节点（事实：边事实），开始日期（结束日期，可选））" 