APP_HOST=0.0.0.0
APP_PORT=8888
# 雪花算法配置
SNOWFLAKE_MACHINE_ID=1

# Redis连接配置
REDIS_HOST=aliyuncs.com
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=***
REDIS_USERNAME=redis_test

# Redis连接池配置
REDIS_MAX_CONNECTIONS=100
REDIS_SOCKET_TIMEOUT=5
REDIS_CONNECT_TIMEOUT=5
REDIS_RETRY_ON_TIMEOUT=True

# Redis缓存配置
REDIS_DEFAULT_TIMEOUT=86400
REDIS_KEY_PREFIX=ai_game_chat:
REDIS_ENABLE_CHARACTER_PROMPT_CACHE=True
REDIS_ENABLE_SYSTEM_CONFIG_CACHE=True
REDIS_ENABLE_LLM_PROVIDER_CACHE=True

# 缓存过期时间配置（秒）
REDIS_CHARACTER_PROMPT_TIMEOUT=86400
REDIS_SYSTEM_CONFIG_TIMEOUT=86400
REDIS_LLM_PROVIDER_TIMEOUT=86400

# 其他应用配置
EMBEDDING_MODEL_DEVICE=cpu  # 或 cuda
EMBEDDING_MODEL_NAME=BAAI/bge-small-zh-v1.5

# 数据库基本配置
DB_ENGINE=mysql
DB_HOST=tencentcdb.com
DB_PORT=20034
DB_USER=root
DB_PASSWORD=***
DB_NAME=***
DB_CHARSET=utf8mb4

# 数据库连接池配置
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=20
DB_POOL_RECYCLE=1800
DB_POOL_TIMEOUT=30
DB_POOL_PRE_PING=True

# 数据库调试配置
DB_ECHO=False
DB_ECHO_POOL=False

# 数据库连接超时配置
DB_CONNECT_TIMEOUT=10